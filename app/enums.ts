export enum FieldType {
  Text = 'text-field',
  Password = 'password-field',
  Number = 'number-field',
  Dropdown = 'dropdown-field',
}

export enum ButtonType {
  Primary = 'primary',
  Secondary = 'secondary',
  Tertiary = 'tertiary',
}

